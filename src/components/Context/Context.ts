import { createContext } from "react";

export const TitleStateContext = createContext(false);
export const LoginFormContext = createContext(false);
export const SignupFormContext = createContext(false);
